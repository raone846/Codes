#include<bits/stdc++.h>
using namespace std;
int main(){
	int t;
	cin>>t;
	while(t--){
		int n,num;
		cin>>n;
		vector<int> vect;
		for(int i=0;i<n;++i){
			cin>>num;
			vect.push_back(num);
		}
		cin>>num;
		if(num>vect[vect.size()-1])
		for(int i=0;i<n;i++){
			if(arr[i]>)
		}
	}
}
